# ServerMaster for Windows

This directory is reserved for the native Windows interface and its Visual
Studio solution. It will consume the same documented ServerMaster data formats
and behavior without requiring a permanent platform branch.
